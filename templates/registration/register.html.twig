{#{% extends 'base.html.twig' %}#}

{#{% block title %}Register{% endblock %}#}

{#{% block body %}#}
{#    {% for flashError in app.flashes('verify_email_error') %}#}
{#        <div class="alert alert-danger" role="alert">{{ flashError }}</div>#}
{#    {% endfor %}#}

{#    <h1>Register</h1>#}

{#    {{ form_start(registrationForm) }}#}
{#        {{ form_row(registrationForm.username) }}#}
{#        {{ form_row(registrationForm.plainPassword, {#}
{#            label: 'Password'#}
{#        }) }}#}
{#        {{ form_row(registrationForm.agreeTerms) }}#}

{#        <button type="submit" class="btn">Register</button>#}
{#    {{ form_end(registrationForm) }}#}
{#{% endblock %}#}

{# Compied from EasyAdminBundle login #}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="robots" content="noindex, nofollow, noarchive, nosnippet, noodp, noimageindex, notranslate, nocache" />
    <meta content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" name="viewport">
    <meta name="generator" content="EasyAdmin" />

{#    <title>{{ block('page_title')|striptags|raw }}</title>#}

    {% block head_stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/easyadmin/app.css') }}">
    {% endblock %}

    {% block head_javascript %}
        <script src="{{ asset('bundles/easyadmin/app.js') }}"></script>
    {% endblock head_javascript %}
</head>

<body id="{% block body_id %}{% endblock %}" class="ea page-login">
<div class="login-wrapper">
    <section class="content">
        <header class="main-header mb-4">
            <div id="header-logo">
                <div class="logo">Register</div>
            </div>
        </header>

        {% for flashError in app.flashes('verify_email_error') %}
            <div class="w-100 alert alert-danger">{{ flashError }}</div>
        {% endfor %}

    {{ form_start(registrationForm) }}
        <div class="form-group field-text">
            {{ form_row(registrationForm.username) }}
        </div>
        <div class="form-group field-text">
            {{ form_row(registrationForm.plainPassword, {
                label: 'Password'
            }) }}
        </div>
        <div class="form-group field-text">
            {{ form_row(registrationForm.agreeTerms) }}
        </div>

        <div class="form-group field-button">
            <button type="submit" class="btn btn-primary btn-lg btn-block">Register</button>
        </div>
    {{ form_end(registrationForm) }}
    </section>
</div>
</body>
</html>